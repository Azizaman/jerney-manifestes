apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: backend
spec:
  clusterIP: None   # 🔥 headless service (required for StatefulSet)
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432